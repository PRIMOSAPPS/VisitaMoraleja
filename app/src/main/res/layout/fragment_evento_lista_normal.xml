<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout_lateral"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/FrameLayout1"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFFFFF"
        tools:context="com.primos.visitamoraleja.EventoListaNormalActivity$PlaceholderFragment" >

        <Button
            android:id="@+id/btnMostrarNotificaciones"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_gravity="top"
            android:background="@color/btnVerNotificaciones"
            android:onClick="mostrarNotificaciones"
            android:text="@string/ver_notificaciones" />
        
        <ListView
            android:id="@android:id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="bottom"
            android:layout_below="@+id/btnMostrarNotificaciones"
            >
        </ListView>
    </RelativeLayout>

    <!-- The navigation drawer -->
    <ListView
        android:id="@+id/menuLateralListaSitios"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/menu_lateral_color_fondo"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp" />

</android.support.v4.widget.DrawerLayout>